package domain.usecase;
import domain.model.Mentee;
import domain.model.Team;


//class IsMenteeInTeam(
   //     private val mentees: List<Mentee>,
  //      private val teams: List<Team>
//) {
   // fun execute(menteeId: String, teamName: String): Boolean {
     //   val mentee = mentees.find { it.id == menteeId } ?: return false
    //    return teams.find { it.teamId == mentee.teamId }?.teamName == teamName
  //  }
//}



